package worker.exchange.action;

import java.util.Map;

import models.SmartExchanges;
import models.SmartInterface;
import Service.unicom.dto.IFResponse;
/**
 * 发起订购
 * @author xuml
 *
 */
public interface Order {
	IFResponse doOrder(SmartExchanges record,SmartInterface si,Map<String,Object> ext);
	IFResponse doOrder(SmartExchanges record,SmartInterface si);
}
